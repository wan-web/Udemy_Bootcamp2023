<%- include('partials/head') %>

  <%- include('partials/navbar')%>

  <h1>Your random number is <%= num %></h1>
  <% if(num % 2 === 0) { %>
    <h1>That is an even number!</h1>
  <% } else { %>
    <h2>That is an odd number!</h2>
  <% } %>

  <h3>That is an <%= num%2===0 ? 'EVEN' : 'ODD' %> number!</h3>
  <%- include('partials/footer') %>